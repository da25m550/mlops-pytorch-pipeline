# =============================================================================
# requirements/serve.txt
# Pinned dependencies for the serving (inference) container.
# Intentionally minimal — no training-only libraries (tensorboard, etc.).
# Install with: pip install --no-cache-dir -r requirements/serve.txt
# =============================================================================

# ── Core ML (CPU-only wheel to keep image small) ─────────────────────────────
# Using the CPU-only index for torch to avoid pulling the ~2 GB CUDA wheel.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# ── Config & utilities ───────────────────────────────────────────────────────
PyYAML==6.0.2
numpy==1.26.4

# ── Image I/O ────────────────────────────────────────────────────────────────
Pillow==10.4.0

# ── Web framework & ASGI server ──────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.3
python-multipart==0.0.9

# ── HTTP client (used in integration tests / health-check scripts) ────────────
httpx==0.27.0
